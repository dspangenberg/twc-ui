---
title: FormTextField
description: A form-integrated text field that allows users to enter plain text values with automatic error handling and form context integration.
docUrl: docs/components/react-aria/text-field
---

<CombiDemoCodePreview codePath="form-text-field/default.tsx" demoPath="demos/form-text-field/default" />

## Installation
<Installation registry="form-text-field" />

## Features

- Seamless form integration with automatic error handling
- Real-time validation feedback from form context
- Accessibility-first design with proper ARIA attributes
- Auto-select text on focus for better UX
- Support for both controlled and uncontrolled usage
- Integration with Laravel validation errors

## Usage

### Basic Usage

<SourceCode hideHeader={true} code={`import { Form, FormTextField, useForm } from '@/components/twc-ui'

const form = useForm('my-form', 'POST', '/submit', {
  name: '',
  email: ''
})

<Form form={form}>
  <FormTextField
    name="name"
    label="Full Name"
    isRequired={true}
    placeholder="Enter your full name"
    description="Please enter your legal name as it appears on documents"
  />
  <FormTextField
    name="email"
    label="Email Address"
    type="email"
    autoComplete="email"
    placeholder="your.email@example.com"
  />
</Form>`} />

### With Custom Error Handling

<SourceCode hideHeader={true} code={`<FormTextField
  name="username"
  label="Username"
  errorMessage={customError}
  isRequired={true}
  onChange={(value) => validateUsername(value)}
/>`} />

## Props

### Required Props

| Prop | Type | Description |
|------|------|-------------|
| `name` | `string` | The form field name for identification and validation |

### Optional Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `string` | - | Field label displayed above the input |
| `value` | `string \| null \| undefined` | - | Controlled text field value |
| `onChange` | `(value: string \| null) => void` | - | Change handler callback |
| `onBlur` | `() => void` | - | Blur handler callback |
| `description` | `string` | - | Helper text displayed below the field |
| `errorMessage` | `string` | - | Custom error message (overrides form context) |
| `isRequired` | `boolean` | `false` | Whether the field is required (shows red asterisk) |
| `placeholder` | `string` | `''` | Input placeholder text |
| `autoComplete` | `string` | `'off'` | Browser autocomplete behavior |
| `type` | `string` | `'text'` | Input type (text, email, tel, url, etc.) |
| `className` | `string` | - | Additional CSS classes |
| `isDisabled` | `boolean` | `false` | Whether the field is disabled |
| `isReadOnly` | `boolean` | `false` | Whether the field is read-only |
| `autoFocus` | `boolean` | `false` | Whether to auto-focus the input |

## Form Integration

The FormTextField automatically integrates with your form context and handles:

- **Error Display**: Shows validation errors from the form context
- **Error Variants**: Respects form-level `errorVariant` setting ('form' or 'field')
- **State Management**: Connects to form data and validation state
- **Laravel Integration**: Automatically handles Laravel validation error formatting

### Error Handling

The component prioritizes custom error messages, falling back to form context errors:

```tsx
// Custom error takes precedence
<FormTextField
  name="email"
  errorMessage="This email is already taken"
/>

// Falls back to form context errors
<FormTextField
  name="email"
  // Will automatically show errors from form.errors.email
/>
```

## Styling

The component uses Tailwind CSS classes with consistent styling:

- **Container**: Flex column layout with gap
- **Input**: Rounded border, focus states, error states
- **Error**: Red text for error messages
- **Focus**: Blue border with ring effect
- **Disabled**: Reduced opacity and cursor changes

## Accessibility

- Proper ARIA attributes for form validation
- Screen reader support for error messages
- Keyboard navigation support
- Auto-focus behavior on input selection
- Semantic HTML structure

## Examples

### Required Field with Validation

<SourceCode hideHeader={true} code={`<FormTextField
  name="requiredField"
  label="Required Field"
  isRequired={true}
  placeholder="This field is required"
  description="Please provide a value for this required field"
/>`} />

### Email Input with Autocomplete

<SourceCode hideHeader={true} code={`<FormTextField
  name="email"
  label="Email Address"
  type="email"
  autoComplete="email"
  placeholder="name@example.com"
  description="We'll never share your email with anyone else"
/>`} />

### Numeric Input

<SourceCode hideHeader={true} code={`<FormTextField
  name="age"
  label="Age"
  type="number"
  placeholder="Enter your age"
  min="1"
  max="120"
/>`} />

### Disabled Field

<SourceCode hideHeader={true} code={`<FormTextField
  name="disabledField"
  label="Disabled Field"
  value="This field is disabled"
  isDisabled={true}
  description="This field cannot be edited"
/>`} />
